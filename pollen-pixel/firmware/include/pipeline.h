#ifndef POLLEN_PIXEL_PIPELINE_H
#define POLLEN_PIXEL_PIPELINE_H

#include "device.h"

void pp_pipeline_run(const pp_capture_t *capture, pp_report_t *report);
void pp_make_demo_capture(pp_capture_t *capture);

#endif
